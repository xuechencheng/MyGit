27	结实Vim的命令行模式
按下什么，Vim切换到命令行模式？
:
从命令行模式返回普通模式
Esc
在命令行模式执行的命令又称为什么？
Ex命令
其他激活命令行模式的方法
/ <C-r>=
常见的Ex命令，读写文件，创建新标签页，分割窗口或操作参数列表以及缓冲区列表的命令
:edit和:write	:tabnew		:split	:prev/:next		:bprev/:bnext
Vim几乎所有功能都提供了响应的Ex命令，如何获得完整列表
:h ex-cmd-index
命令行模式快捷键也是可以使用的，删除至上个单词的开头及行首的快捷键是什么？
<C-w>	<C-u>
插入键盘找不到的字符
<C-v>	<C-k>
把任意寄存器的内容插入到命令行内
<C-r>{register}
Ex命令比用普通模式命令的优点是什么？
普通模式一般操作当前行，Ex命令可以操作任意行
Ex命令脱颖而出的功能是什么？
能够在多行上同时执行的能力
vi的意思
visual命令激活
删除指定范围内的行，到寄存器x中
:[range]delete [x]
复制指定范围的行
:[range]yank [x]
在指定行后粘贴寄存器x中的内容
:[line]put [x]
在指定范围内的行拷贝到指定的行下
:[range]copy{address}
在指定范围内的行移动到指定的行下
:[range]move{address}
连接指定范围内的行
:[range]join
对指定范围内的每一行指定普通模式命令
:[range]normal{commands}
对指定范围内出现pattern的地方替换为string
:[range]substitute/{pattern}/{string}/[flags]
对指定范围内匹配pattern的所有行，在其上执行Ex命令
:[range]global/{pattern}/[cmd]

28	在一行或多个连续行上执行命令
Ex命令中，我们可以用什么指定范围的开始和结束位置？
行号，位置标记或查找模式
打印第一行内容
:1	:print
打印最后一行内容
:$	:p
打印和删除第三行
:3p	:3d
普通模式下删除第三行
3G	dd
打印2到5行
:2,5p
当前行地址
.
打印当前位置到文件末尾间的所有行
:.,$p
代表当前文件中的所有行
%
打印全文
:%p
把每一行的第一个A替换成B
:%s/A/B/
用高亮选区选定一个范围，如果按下:按键，命令行上就会预先填充一个范围
:'<,'>
打印选中区域的内容
:'<,'>p
用模式指定范围，打印html标签内的内容
:/<html>/,/<\/html>/p
用偏移对地址进行修正，打印html标签内的内容，但是不包含标签
:/<html>/+1,/<\/html>/ -lp
打印2到5行内容
:2	:.,.+3p
文件的第一行
1
文件的最后一行
$
文件的第一行上方
0
光标所在行
.
包含位置标记m的行
'm
高亮选区的起始行
'<
高亮选区的结束行
'>
整个文件
%

29 使用':t'和':m'命令复制和移动行
:copy命令的简写是什么
:t
把第6行的内容复制到当前行的下一行
:6t.
copy的命令格式
:[range]copy{address}
把当前行复制到第6行下发
:t6
类似于yyp
:t.
把当前行复制到文本结尾
:t$
把高亮选中的行复制到文件开头
:'<,'>0
普通模式下，返回原来的位置
<C-o>
:move可以简写成什么
:m
将选中的高亮区域，移动到文件尾
:'<,'>m$
执行上一个Ex命令
@:

30 在指定范围上执行普通模式命令
将当前行到最后一行，每行结尾都添加一个分号
A;<Esc>	jVG	:'<,'>normal .
对高亮选区的每一行，执行普通模式下的.命令
:'<,'>normal .
每一行结尾都添加一个;
:%normal A;
把文件注释掉
:%normal i//

31 重复上次的Ex命令
重复上次的Ex命令
@:
遍历缓冲区列表的条目，正向移动和反向移动
:bn	:bp
什么寄存器总是保存着最后执行的命令行命令
:寄存器
在运行过一次@:后，可以用什么命令重复它
@@
如果我们执行太多次@:命令以至于错过了目标，要怎么样才能改变方向往回跳呢？
:bp

32 自动补全Ex命令
命令行上使用什么键自动补全命令
Tab
查看有哪些补全项
<C-d>
如果有多个补全项，Tab会怎么处理
循环遍历

33 把当前单词插入到命令行
在Vim命令行下，什么映射项会复制光标下的单词并把它插入到命令行中,，一般用于替换字符串。
<C-r><C-w>

34 回溯历史命令
保持提示符为空的情况下按什么键？
上下方向键
如果要回溯help开头的历史命令，怎么办
输入help然后使用方向键
修改历史记录数量的设置值
set history=200
如果要查看查找命令的历史记录怎么办？
按/调出查找提示符之后，然后使用方向键
调出命令行窗口的命令
q:
命令行窗口的每行内容对应着什么
命令历史中的一个条目
可以修改历史命令内容，或者是将多个命令组合起来，在什么模式？
命令行窗口
关闭命令行窗口命令
:q或者<CR>

35 运行shell命令
在Vim命令行模式中，给命令加一个什么可以调用外部程序
叹号前缀
查看当前目录的内容
:!ls
在Vim命令行模式中，什么符号代表当前文件名？
%
:!{cmd}这种语法适用于执行一次性命令，但是如果想要执行多条命令怎么办？可以执行什么命令启动一个交互的shell回话。
:shell
退出上面的shell会话
exit
如果需要执行shell命令，可以把Vim挂到后台，使用什么命令？
<C-z>
查看作业列表
jobs
唤醒一个被挂起的进程
fg
把cmd命令的输出读入到当前缓冲区中
:read !{cmd}
把缓冲区的内容作为制定cmd的标准输入
:write !{cmd}
将第二行到结尾的内容按照第二列进行排序
:2,$!sort -t ',' -k
启动一个shell
:shell
在shell中执行cmd
:!cmd
在shell中执行cmd，并把其标准输出插入到光标下方
:read !cmd
在shell中执行cmd，以range作为其标准输入
:[range]write !cmd
使用外部程序filter过滤制定的range
:[range]!{filter}