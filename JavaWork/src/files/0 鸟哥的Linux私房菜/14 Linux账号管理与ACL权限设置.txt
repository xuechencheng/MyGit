Linux主机只认识id，id和账号的对应关系在哪个文件中
/etc/passwd
每个登录用户至少有两个ID，用户id和用户组id，简写为什么
UID和GID
用户登录之后，首先读取哪两个文件获取uid和gid
/etc/passwd /etc/group
然后使用UID去哪个文件验证密码
/etc/shadow
/etc/passwd里面的bin，daemon，adm，nobody等账号是什么账号
系统账号
/etc/passwd文件root:x:0:0:root:/root:/bin/bash各个字段的含义
账号 密码 UID GID 用户信息 主文件夹 Shell
系统管理员的UID是多少
0
系统账号UID范围
1-499
/etc/shadow的内容root:*:18037:0:99999:7:::各个字段含义
账号名称 密码 密码更改日期 密码不可更改天数 密码需要重新更改天数 密码需要更改期限前警告天数 密码过期后宽限时间 账号失效日期 保留
/etc/group文件结构 root:x:0:root
用户组名称 用户组密码 GID 用户组的账号
用户登录时，立刻拥有的用户组称为什么
初始用户组
创建的文件和目录属于哪个用户组，取决于什么
有效用户组
查看支持的用户组的命令
groups
将有效用户组切换为users
newgrp users
/etc/gshadow内容 root:::root各字段含义
用户组名 密码 管理员账号 用户组所属账号
gshadow最大的功能是什么
创建组管理员
让用户组管理员管理组员信息，但是现在可以使用什么命令代替，这个功能很少使用了
sudo
添加用户的命令
useradd
添加用户vbird
useradd vbird
创建系统账户vbird
useradd -r vbird
使用useradd命令会给更改哪些文件
/etc/passwd /etc/shadow /etc/group /home
查看useradd添加用户的默认值
useradd -D
创建新用户时候，默认值的配置文件是哪个
/etc/default/useradd
创建新用户，系统会创建一个与账号一样的用户组给用户，这是因为什么？
私有用户组机制
密码过期后是否会失效的设置值
INACTIVE
账号失效的日期
EXPIRE
默认的shell程序文件名
SHELL
让用户无法登陆shell，可以把SHELL设置为什么
/sbin/nologin
新用户的主文件夹内的东西参考哪个目录的
/etc/skel
UID和GID还参考什么文件
/etc/login.defs
useradd这个程序在创建账号时，至少会参考哪些文件
/etc/default/useradd /etc/login.defs /etc/skel/*
创建的新用户，无法直接使用，需要进行什么操作？
设置密码
修改密码的命令
passwd
给vbird账号设置密码
passwd vbird
账号的复杂度要求写在哪个文件里
/etc/pam.d/passwd
使用stdin设置vbird用户的密码为123456
echo "123456" | passwd --stdin vbird
管理vbird账号，使其密码60天更改，10天失效密码设置
passwd -x 60 -i 10 vbird
使vbird账号失效
passwd -l vbird
使vbird账号恢复
passwd -u vbird
查看vbird密码信息
passwd -S vbird
更详细的查看密码信息的命令
chage
列出vbird的详细密码参数
chage -l vbird
让新建的用户vbird第一次登录时更改密码
chage -d 0 vbird
对账号相关数据进行微调的命令
usermod
删除用户连同用户的主文件夹
userdel -r username
如果只是让某个用户暂时不可用修改什么文件
/etc/shadow第八个字段设置为0
查看登录等指纹信息
finger [-s] username
类似于finger的命令
chfn chsn
查询默认或自己UID/GID等信息的命令
id
与用户组内容有关的两个文件
/etc/group /etc/gshadow
新建用户组的命令
groupadd
对group参数进行修改的命令
groupmod
删除用户组
groupdel
管理用户组的命令
gpasswd
gpasswd将主控权交给后面的用户管理
A
将某些账户加入这个用户组当中
N
将密码删除
r
设置ACL支持文件
/etc/fstab
取得某个文件/目录的ACL设置项目
getfacl
设置某个文件/目录的ACL设置项目
setfacl
使用acl设置vbird用户对于acl_test文件具有rx权限
setfacl -m u:vbird:rx acl_test
查看acl_test权限
getfacl acl_test
使用acl设置mygroup组对于acl_test文件的rx权限
setfacl -m g:mygroup:rx acl_test
系统日常操作使用什么账号
一般账号
让一般用户转变身份成为root，有哪两种方式？
su - sudo
使用su -登入root账号使用的是什么shell？
login-shell
使用su登入root账号使用的是什么shell？
non-login-shell
退出切换的身份，回到刚开始的身份使用什么命令
exit
只使用root身份执行一次命令
su - -c
su和sudo哪个需要知道别人的密码？
su
哪个文件内的用户才可以执行sudo？
/etc/sudoers
sudo的参数，后面可以接想要切换的用户，若无此项就代表身份切换为root
u
修改/etc/sudoers的命令
visudo
/etc/sudoers的一行内容 root ALL=(ALL) ALL 四列的含义
用户账号 登录者的来源主机名 可切换的身份 可执行的命令
设置用户组权限时，最左边时什么符号
%
设置第四个字段为免除密码输入
NOPASSWD:ALL
让其他用户不能执行修改密码的命令，需要设置第四列为什么
!/usr/bin/passwd
为了方便一次处理多个账号的sudo权限，可以使用什么功能？
别名
系统账号使用的是哪个shell？
/sbin/nologin
系统账号可以使用bash或其他shell来登录系统吗？
不可以
一套验证机制，可以提供给其他程序调用
pam
PAM模块会去哪里寻找与程序比如passwd同名的配置文件
/etc/pam.d
/etc/pam.d/passwd中一行 auth include system-auth分别是什么含义
验证类别 控制标准 PAM模块
哪四种验证类型
auth account session password
哪四种验证的控制标志
required requisite sufficient optional
查看用户的登录时间的命令
last
查看目前登录的用户的两个命令
w who
知道每个账号的最近登录时间
lastlog
和其他用户聊天的命令
write
给所有用户发送消息的命令
wall
发邮件的命令是什么
mail
给vbird用户发送一份标题为hello，内容为filename内容的邮件
mail vbird -s "hello" < filename
收取邮件的命令是什么
mail
检查/etc/passwd配置信息的命令
pwck
将/etc/passwd内的账号与密码移动到/etc/shadow中的命令
pwconv
