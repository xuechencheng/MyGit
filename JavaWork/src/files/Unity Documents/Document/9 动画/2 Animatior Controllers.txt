Animator Controller

Animator Controller资源
当您准备好使用动画剪辑时，需要使用什么使这些剪辑结合在一起？
Animator Controller
最终会通过连接什么组件（其中引用了Animator Controller）将Animator Controller应用于对象？
Animator

Animator 窗口
Animator窗口有两个主要部分：主要网格化布局区域以及左侧什么和什么面板？
Layers和Parameters
可在单个动画控制器中同时运行多个动画层，每个动画层由一个单独什么控制？此情况的常见用途是在控制角色一般运动动画的基础层之上设置一个单独层来播放上身动画。
状态机

动画状态机

状态机基础
状态集合、状态过渡条件以及记录当前状态的变量放在一起，形成了一个什么？
状态机

动画参数
什么是在Animator Controller中定义的变量，可从脚本访问这些变量并向其赋值？这是脚本控制或影响状态机流程的方法。
动画参数
可通过什么更新参数的值，然后从脚本访问参数以便可改变音效的音高？
动画曲线
动画参数可分为哪四个基本类型？
int float bool trigger

状态机过渡
Animator窗口中的每个视图都有哪两个节点？
进入(Entry)和退出(Exit)
因为状态机始终具有什么状态，所以始终会有从进入节点分支到默认状态的默认过渡？
默认状态

状态机行为
什么是一类特殊脚本，与将常规 Unity 脚本 (MonoBehaviour) 附加到单个游戏对象类似，您可以将 StateMachineBehaviour 脚本附加到状态机中的单个状态。
状态机行为
可编写一些将在状态机进入、退出或保持在特定状态时执行的代码。这意味着您不必编写自己的逻辑来测试和检测什么的变化？
状态
在状态机中选择状态，然后在检视面板中使用什么按钮来选择现有的 StateMachineBehaviour 或创建新行为？
Add Behaviour
参考文档：StateMachineBehaviour类

子状态机
角色通常具有包含若干阶段的复杂动作。合理的做法是识别单独阶段并将单独状态用于每个阶段，而不是用什么来处理整个动作？
单个状态
Mecanim 比这更进一步，允许您将一组状态折叠为状态机图中的单个指定项。这些折叠的状态组称为什么？
子状态机
若要创建子状态机，可右键单击Animator Controller窗口中的空白空间，并从上下文菜单中选择什么？子状态机在编辑器中用细长六边形表示以区别于正常状态。
Create Sub-State Machine
双击六边形时将清理编辑器，让您编辑什么，好像它本身就是一个完全独立的状态机？
子状态机
子状态机只是一种在编辑器中直观地折叠一组状态的方式，因此在过渡到子状态机时，必须选择要连接到子状态机的什么？
哪个状态
子状态机中有一个名称以什么开头的额外状态，该状态表示“外部世界”，这是在视图中包含子状态机的状态机？
Up
如果添加从子状态机中的状态到Up状态的过渡，系统将提示您选择要连接到闭包状态机的什么？
哪个状态

动画层
Unity使用什么来管理不同身体部位的复杂状态机？相应的示例为，您有一个用于行走/跳跃的下身层，还有一个用于投掷物体/射击的上身层。
动画层
单击窗口右侧的什么按钮可显示该层的设置？
齿轮
在每一层上，您可以指定什么（应用动画的动画模型的一部分）以及混合类型？Override 表示将忽略其他层的信息，而 Additive 表示将在先前层之上添加动画。
遮罩
什么属性用于指定此层上使用的遮罩？
Mask 
有时，能够在不同层中复用同一状态机是很有用的。例如，如果想要模拟“受伤”行为，并生成“受伤”状态下的行走/奔跑/跳跃动画，而不是“健康”状态下的动画，您可以单击其中一个层上的什么复选框，然后选择要同步的层？随后状态机的结构便会相同，但状态使用的实际动画剪辑不同。
Sync
这意味着同步的层根本没有自己的状态机定义，而是同步层状态机的一个实例。在同步层视图中对状态机的布局或结构所做的任何更改（例如，添加/删除状态或过渡）都是针对同步层的什么进行的？
源
同步层的唯一独特更改是每个状态内使用的什么？
选定动画
通过什么复选框，Animator 可调整同步层中每个动画所需的时间（由权重决定）。如果取消选中，则会调整同步层上的动画。
Timing
Layers 侧边栏中显示什么符号，表示该层是同步层？
S

Solo 和 Mute 功能
Mute表示将禁用过渡。而什么功能将启用过渡，并与源自同一状态的其他过渡有关？
Solo
独奏的过渡将以绿色显示，而静音的过渡将以什么颜色显示？
红色
基本的经验法则是，如果勾选一个 Solo，源自该状态的其余过渡将怎么样？
静音
如果同时勾选 Solo 和 Mute，则什么将优先执行？
Mute

目标匹配
通常在游戏中可能出现以下情况：角色必须以某种方式移动，使得手或脚在某个时间落在某个地方。您可以使用什么函数来处理此类情况？
Animator.MatchTarget
参考例子：Animator.MatchTarget使用

反向动力学
大多数动画是通过将骨架中的关节角度旋转到预定值来生成的。子关节的位置根据父关节的旋转而改变，因此可从父关节包含的各个关节的角度和相对位置来确定关节链的终点。这种构建骨架的方法被称为什么？
正向动力学
在空间中选择一个位置后，向后找到一种有效的关节定位方法，使终点落在该位置。如果您希望角色触摸位于用户选定位置的对象或让角色的双脚牢牢扎入不平坦的表面，这种方法可能很有用。此方法称为什么？
反向动力学
在 Animator 窗口的 Layers 面板中，单击层的齿轮设置图标，并选中弹出框中的什么复选框？
IK Pass
编程实例：使用IK范例

根运动 - 工作原理
身体变换是角色的质心。它用于Mecanim的重定向引擎，并提供最稳定的移位模型。身体方向是相对于什么形姿势的下身和上身方向的平均值？
Avatar T
身体变换和方向存储在什么中？它们是动画剪辑中存储的唯一世界空间曲线。所有其他：肌肉曲线和 IK（反向动力学）目标（手和脚）都是相对于身体变换进行存储的。
动画剪辑
根变换是身体变换在什么平面上的投影，并在运行时计算？在每一帧都会计算根变换的变化。变换的此变化随后应用于游戏对象以使其移动。
Y
动画剪辑编辑器设置Root Transform Rotation、Root Transform Position (Y) 和Root Transform Position(XZ)，可让您从身体变换控制什么的投影？
根变换
根据这些设置，身体变换的某些部分可能会转移到根变换。例如，您可以决定是否希望运动 Y 位置成为根运动（轨迹）的一部分或姿势（身体变换）的一部分称为什么？
Baked into Pose
Root Transform Rotation中的Bake into Pose表示方向将保持在身体变换（或姿势）上。根方向将是常量，增量方向将是标识。这意味着游戏对象根本不会被什么旋转？
动画剪辑 (AnimationClip)
只有具有什么根方向的动画剪辑才应使用此选项？
相似的开始和停止
您将在UI上看到绿色指示灯，表示动画剪辑是合理的候选项。合适候选项将是什么样的？
直走或奔跑
什么选项用于设置剪辑的方向？
Based Upon
使用什么会将剪辑定向以跟随身体的向前矢量？此默认设置适用于大多数动作捕捉数据（如行走、奔跑和跳跃），但是对于诸如扫射一样的运动（此类情况下，运动垂直于身体的向前矢量），此设置将会失败。
Body Orientation
在这些情况下，可使用什么设置来手动调整方向？
Offset
最后还可选择什么设置，此设置会自动添加位于导入的剪辑中的创作偏移量。此设置通常与关键帧数据一起使用以遵循美术师设定的方向。
Original
什么为Based Upon选择该选项时用于输入偏移量？
Offset
Root Transform Position(Y)中，什么选项表示运动的 Y 分量将保留在身体变换（姿势）上。根变换的Y分量将是常量，增量根位置Y将为0。这意味着此剪辑不会更改游戏对象高度。您再次看到绿色指示灯，表示剪辑是将Y运动烘焙到姿势中的合理候选项
Bake Into Pose
大多数动画剪辑将启用此设置。只有会改变游戏对象什么属性的剪辑才应该将此设置关闭，比如向上跳或向下跳？
高度
Root Transform Position (XZ)，Bake Into Pose 通常用于“空闲”状态，此情况您会希望将增量位置 (XZ) 强制设置为什么？此选项将阻止多次评估后小增量漂移发生积累。
0

教程：为“原地就位”的人形动画编写根运动脚本（待续）
