纹理
纹理是覆盖或包裹游戏对象以赋予它们视觉效果的图像或什么文件？
电影文件
Unity 会将 3D 项目的 Assets 文件夹中的所有图像或电影文件识别为纹理，在2D项目中保存为什么？
精灵
参考文档：纹理导入器
默认情况下会隐藏一些不太常用的属性。在Inspector窗口中单击什么即可查看这些属性？
Advanced

导入纹理
理想情况下，纹理尺寸大小应该是每边为多大？
2的幂
纹理不必是什么形状，也就是宽度可与高度不同？
正方形
Unity允许使用NPOT（非 2 的幂）纹理大小；但是，NPOT 纹理大小通常需要稍微多一点的内存，并且 GPU 的采样速度可能更慢，因此，只要有可能，最好使用什么大小以提高性能？
2的幂
如果平台或GPU不支持NPOT纹理大小，Unity会对纹理进行什么处理？
缩放和填充以达到下一个2的幂大小
通常，只应将NPOT大小用于什么用途？
GUI
您可以使用纹理导入器的Advanced部分中的什么选项在导入时放大NPOT纹理资源？
Non Power of 2
将2D纹理映射到3D模型上时，3D建模应用程序会执行一种称为什么的包裹？
UV贴图
在Unity内部，可使用什么来缩放和移动纹理？
材质
什么是图像逐渐减小版本的列表，用于优化实时3D引擎的性能？
Mipmap
什么对象使用较小的纹理版本？
远离摄像机
使用Mipmap会多占用多大的内存，但不使用它们会导致巨大的性能损失？
33%
游戏中的纹理应当始终使用Mipmap；唯一的例外是什么？Mipmap对于避免许多形式的纹理锯齿和闪烁也是必不可少的。
变小的纹理（例如，GUI纹理、天空盒、游标和剪影）
什么由法线贴图着色器用于使简单多边形模型看起来好像包含更多细节？
法线贴图
要将纹理用于反射贴图（例如在反射探针或立方体贴图的天空盒中），请将Texture Shape设置为什么？
Cube
什么可提高从掠射角观察时的纹理质量？此渲染在显卡上非常消耗资源。提高各向异性水平通常对于地面和地板纹理而言是一种很好的做法。
各向异性过滤
请使用什么强制对所有纹理进行各向异性过滤或完全禁用该功能？
质量设置 (Quality Settings) 

纹理类型
参考文档：纹理类型

特定于平台的覆盖的纹理压缩格式
虽然Unity支持许多常见的图像格式作为导入纹理的源文件（例如JPG、PNG、PSD和TGA），但在3D图形硬件（如显卡或移动设备）的实时渲染过程中会使用这些格式吗？
不会
3D图形硬件要求纹理以什么格式进行压缩，这些格式针对快速纹理采样进行了优化。各种不同的平台和设备都有自己不同的专有格式。
专门
默认情况下，Unity Editor会自动将纹理转换为最合适的格式，以匹配您选择的构建目标吗？
会
在大多数平台上，可选择许多不同的受支持的纹理压缩格式。Unity 为每个平台设置了一些默认格式，但在某些情况下，您可能希望怎么处理？
覆盖默认值并为某些纹理选择不同的压缩格式
要为每个平台应用自定义设置，请使用纹理导入器(Texture Importer)设置默认选项，然后怎么处理？
使用特定于平台的覆盖(Platform-specific overrides)面板覆盖特定平台的默认设置
使用目标平台不支持的纹理压缩格式时，纹理将解压缩为什么并与压缩纹理一起存储在内存中？
RGBA 32
参考文档：各平台支持的压缩格式
除非针对特定硬件（例如 Tegra），否则什么压缩是Android最高效的选项，可提供最佳的质量与文件大小（及相关的内存大小要求）平衡？
ETC2
可对具有Alpha通道的纹理使用什么格式，但仅当构建目标为 Android 且纹理放置在图集上（通过指定打包标签）时才适用？
ETC1

渲染纹理
什么是在运行时创建和更新的特殊类型的纹理？
渲染纹理
要使用渲染纹理，首先要创建一个新的渲染纹理，并指定一个什么以渲染到其中？
摄像机
参考文档：渲染纹理

自定义渲染纹理
自定义渲染纹理是渲染纹理的扩展，允许用户使用什么轻松更新相关纹理？这对于实现各种类型的复杂模拟（例如焦散、雨水效果的波纹模拟、墙上的液体喷溅等）非常有用。
着色器
参考文档：自定义渲染纹理
可通过上下文什么菜单将自定义渲染纹理导出到 PNG 或 EXR 文件（具体取决于纹理格式）？
Export
详情待续

电影纹理（了解）
什么是从视频文件创建的动画纹理？
电影纹理
通过在项目的Assets文件夹中放置视频文件，即可导入视频，随后就能就像什么一样使用视频？
常规纹理

3D 纹理
只能从什么创建3D纹理？
脚本
参考文档：创建3D纹理的脚本

纹理数组
请参阅高级 ShaderLab 主题。

