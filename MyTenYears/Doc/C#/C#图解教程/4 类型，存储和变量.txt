4 类型，存储和变量
4.1 C#程序是一组类型声明
4.2 类型是一种模板
4.3 实例化类型
4.4 数据成员和函数成员
4.5 预定义类型
参考文档：16种预定义类型
参考文档：预定义类型的取值范围和对应的底层.NET类型
4.6 用户定义类型
4.7 栈和堆
栈是一个内存数组，是一个后进先出的数据结构。
与栈不同，堆里的内存能够以任意顺序存入和移除。CLR的自动垃圾收集器管理堆内存。

4.8 值类型与引用类型
值类型只需要一段单独的内存，用于存储实际的数据。
引用类型需要两段内存：第一段存储实际的数据，总是位于堆中。第二段是一个引用，指向数据在堆中的存放位置，可能存放在堆中也可能存放在栈中。
对于引用类型的任何对象，它所有的数据成员都存放在堆里，无论它们是值类型还是引用类型。
参考文档：C#中的值类型和引用类型

4.9 变量
4.10 静态类型和dynamic关键字
dynamic关键字代表一个特定的C#类型，它知道如何在运行时解析自身。
在编译时，编译器不会对dynamic类型的变量做类型检查。

4.11 可空类型


