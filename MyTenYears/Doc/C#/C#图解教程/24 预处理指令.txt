预处理指令
24.1 什么是预处理指令
预处理指令指示编译器如何处理源代码。例如，某些情况下，我们可能希望编译器忽略一部分代码。

24.2 基本规则
预处理指令必须和C#代码在不同的行。
与C#语句不同，预处理指令不需要以分号结尾。
包含预处理指令的每一行必须以#字符开始。
#define PermiumVersion
#define #undef #if #elif #else #endif 

24.3 #define和#undef指令
#define和#undef指令只能用在源文件的第一行。

24.4 条件编译
有4个指令可以用来指定条件编译：
#if #elif #else #endif 

24.5 条件编译结构

24.6 诊断指令

24.7 行号指令

24.8 区域指令
区域指令允许我们标注和有选择性地命名一段代码。
区域由一个#region指令和其下方某处的一个#endregion指令构成。

24.9 #pragma warning指令